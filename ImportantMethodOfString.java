public class ImportantMethodOfString{
    public static void main(String[] args){
        string input = "Anjali Prajapati"
        int length = input.length();
        //getting character from string using charAt
        char firstChar = input.charAt(0);
        //getting index of character from string.
        // will return the first occured index if the character is present in the string.
        // or else will return -1
        
    }
}